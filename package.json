{
  "name": "bit-test-2",
  "version": "1.0.0",
  "description": "Site publishes at: to be determined",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --port=4006 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Zilvinas",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.9"
  }
}
